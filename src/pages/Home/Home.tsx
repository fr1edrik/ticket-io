import React, { FC } from 'react';
import TicketView from '../../components/TicketView/TicketView';

const Home: FC = () => {
	return (
		<div>
			<TicketView />
		</div>
	);
};

export default Home;
